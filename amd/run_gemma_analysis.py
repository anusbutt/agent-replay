"""PLACEHOLDER — to be replaced by the actual script executed on the AMD pod.

The real file loads Gemma 4 26B via torch + ROCm on the AMD Radeon
(gfx1100) device, reads analysis_prompt.txt, runs inference, and writes
the verdict JSON to gemma4_verdict_on_amd.json.

This placeholder exists so the amd/ evidence layout is complete before the
captured artifacts are committed; it makes no claim by itself.
"""

raise SystemExit(
    "placeholder — replace with the script actually run on the AMD compute pod"
)
